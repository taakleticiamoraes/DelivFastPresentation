public interface PoliticaDeFrete {
    // ================================================================
    // MÉTODO DA INTERFACE
    // ================================================================

    Dinheiro calcularFrete(Pedido pedido);
}